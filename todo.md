## Blockers

## Critical

## Normal

## Backlog

- do not error requiring database creds if --files is used during backup
- do we need ldp dump handler anymore? database.handler
- add a hook system to be able to customize at different points (for sanitize)?
- add git tag to the backup name website_backup?
- install should copy env tokens even if the config file already exists, with confirmation in case of upgrades?

## Notes
